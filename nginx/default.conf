upstream client {
  server client:8000;
}

upstream api {
  server api:3000;
}

server {
  listen 8080;

  location / {
    proxy_pass http://client;
  }

  location /api {
    proxy_pass http://api;
  }
}
